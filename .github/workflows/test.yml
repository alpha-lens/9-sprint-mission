name: Java CI with Gradle and CodeCov

on:
  push:
    branches: [ "main", "이진용", "이진용-sprint8" ]
  pull_request:
    branches: [ "main", "이진용" ]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./discodeit

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Test with Gradle (H2 Mode)
        run: ./gradlew clean test -Dspring.profiles.active=test
        env:
          AWS_S3_ACCESS_KEY: dummy
          AWS_S3_SECRET_KEY: dummy
          AWS_S3_REGION: ap-northeast-2
          AWS_S3_BUCKET: dummy-bucket
          SPRING_BOOT_ADMIN_CLIENT_URL: http://localhost:9090

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true